// Public entry point for the shared protobuf/gRPC code.
//
// The generated sources live in `./generated` and are produced by
// `bun run generate:proto` (see ../generate-proto.sh). This template ships
// empty; once you add `.proto` files to the repository `proto/` directory and
// run codegen, import the generated modules by subpath, for example:
//
//   import { SomeServiceDefinition } from "app-protobuf/your_module/your_file";
//
// You may also re-export the modules you use most from this file.

export {};
